public class AsyncExecutionExample implements Queueable {
    public void execute(QueueableContext context) {
        Contact cojContact = new Contact(LastName='AlokSingh',Email = 'aftabalam@gmail.com');
        insert cojContact;        
    }
}